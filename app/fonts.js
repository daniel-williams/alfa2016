var WebFont = require('webfontloader');

  WebFont.load({
    google: {
      families: ['Roboto Slab', 'Quicksand']
    },
    custom: {
      families: ['icomoon'],
      urls: ['/content/fonts/fonts.css']
    }
  });
